apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lightrag-tls
  namespace: gaspard
spec:
  secretName: lightrag-tls
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - lightrag.${CLUSTER_DOMAIN}
