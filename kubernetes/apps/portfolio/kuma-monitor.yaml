apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: portfolio
  namespace: portfolio
spec:
  config:
    type: http
    name: Portfolio
    url: https://bruno.${PROJECTS_DOMAIN}
    interval: 60
    accepted_statuscodes:
      - "200-299"
      - "300-399"
    notification_name_list:
      - discord
