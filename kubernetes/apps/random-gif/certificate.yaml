apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: random-gif-tls
  namespace: random-gif
spec:
  secretName: random-gif-tls
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - random-gif.${CLUSTER_DOMAIN}
