apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://traefik.github.io/charts
    targetRevision: 37.4.0
    chart: traefik
    helm:
      values: |
        providers:
          kubernetesGateway:
            enabled: true

        gateway:
          enabled: false

        ports:
          web:
            redirections:
              entryPoint:
                to: websecure
                scheme: https

        ingressRoute:
          dashboard:
            enabled: true
            matchRule: Host(`traefik.internal`)
            entryPoints:
              - websecure
            tls:
              secretName: traefik-dashboard-tls

        service:
          type: LoadBalancer

        logs:
          general:
            level: INFO
          access:
            enabled: true

  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik-resources
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/bruno00o/homelab.git
    targetRevision: main
    path: kubernetes/apps/traefik/resources
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
