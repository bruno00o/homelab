apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: umami-tls
  namespace: umami
spec:
  secretName: umami-tls
  issuerRef:
    name: ${CLUSTER_ISSUER}
    kind: ClusterIssuer
  dnsNames:
    - umami.${CLUSTER_DOMAIN}
